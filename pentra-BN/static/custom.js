// custom.js for pentra-BN advanced dashboard
// Add AJAX, live updates, and UI interactivity here

// Example: Fetch bot stats and update dashboard
// setInterval(() => {
//   fetch('/api/stats').then(r => r.json()).then(data => {
//     document.getElementById('total-bots').textContent = data.total_bots;
//     document.getElementById('online-bots').textContent = data.active_bots;
//     document.getElementById('total-commands').textContent = data.total_commands;
//     document.getElementById('last-activity').textContent = data.last_activity || 'Never';
//   });
// }, 5000); 